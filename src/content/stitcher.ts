// ============================================================
// ClaudeWeaver — Visual Stitcher
// Hides split seams when navigating between segments.
// Phase 5 polish — not required for MVP.
// ============================================================

/**
 * When navigating to a segment, hide the continuation prompt injection
 * to make the transition appear seamless.
 * For MVP: simply navigating to the conversation URL is sufficient.
 */
export function stitchSegmentView(_segmentId: string): void {
  // TODO Phase 5: Hide continuation prompt in conversation view
  // This is polish — skip for MVP.
}
